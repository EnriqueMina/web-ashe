<section class="hero">

  <div class="hero-glow"></div>

  <div class="hero-content">

    <p class="hero-category">
      MEDIO COMUNITARIO AFRO
    </p>

    <h1>
      ASHÉ
    </h1>

    <div class="hero-line"></div>

    <h2>
      La Fuerza Cultural
      <span>que mueve el Territorio</span>
    </h2>

    <p class="hero-description">
      Historias, voces, memoria e identidad de las comunidades
      afrodescendientes en Bogotá.
    </p>

    <div class="hero-actions">

      <a href="#historias" class="btn-principal">
        Explorar historias
        <span>→</span>
      </a>

      <a href="/quienes-somos" class="btn-secundario">
        Conoce ASHÉ
      </a>

    </div>

  </div>

  <div class="hero-bottom">

    <div class="hero-scroll">
      <span></span>
      <p>Descubre nuestras historias</p>
    </div>

  </div>

</section>


<style>

  /* ================================
     HERO
  ================================= */

  .hero {
    position: relative;

    min-height: calc(100vh - 75px);

    display: flex;

    align-items: center;
    justify-content: center;

    overflow: hidden;

    background:
      radial-gradient(
        circle at 75% 30%,
        rgba(200, 162, 74, 0.12),
        transparent 32%
      ),
      radial-gradient(
        circle at 20% 80%,
        rgba(168, 87, 43, 0.08),
        transparent 28%
      ),
      #1B1B1B;

    color: #F7F4ED;
  }


  /* ================================
     BRILLO DECORATIVO
  ================================= */

  .hero-glow {
    position: absolute;

    width: 450px;
    height: 450px;

    right: -170px;
    top: -170px;

    border: 1px solid
      rgba(
        200,
        162,
        74,
        0.16
      );

    border-radius: 50%;

    box-shadow:
      0 0 0 60px
      rgba(
        200,
        162,
        74,
        0.025
      ),
      0 0 0 120px
      rgba(
        200,
        162,
        74,
        0.015
      );

    pointer-events: none;
  }


  /* ================================
     CONTENIDO
  ================================= */

  .hero-content {
    position: relative;

    z-index: 2;

    width: min(
      850px,
      calc(100% - 40px)
    );

    text-align: center;

    padding: 45px 0 65px;
  }


  /* ================================
     CATEGORÍA
  ================================= */

  .hero-category {
    margin: 0 0 14px;

    color: #C8A24A;

    font-size: 0.62rem;

    font-weight: 700;

    letter-spacing: 3px;
  }


  /* ================================
     NOMBRE
  ================================= */

  .hero h1 {
    margin: 0;

    color: #C8A24A;

    font-size: clamp(
      4.2rem,
      10vw,
      7rem
    );

    line-height: 0.88;

    font-weight: 800;

    letter-spacing: -4px;
  }


  /* ================================
     LÍNEA
  ================================= */

  .hero-line {
    width: 60px;
    height: 2px;

    margin: 20px auto 22px;

    background: #C8A24A;
  }


  /* ================================
     TÍTULO
  ================================= */

  .hero h2 {
    max-width: 760px;

    margin: 0 auto;

    color: #F7F4ED;

    font-size: clamp(
      1.8rem,
      4vw,
      3.1rem
    );

    line-height: 1.06;

    font-weight: 700;

    letter-spacing: -0.8px;
  }


  .hero h2 span {
    display: block;

    color: #C8A24A;
  }


  /* ================================
     DESCRIPCIÓN
  ================================= */

  .hero-description {
    max-width: 600px;

    margin: 20px auto 0;

    color:
      rgba(
        247,
        244,
        237,
        0.75
      );

    font-size: 0.92rem;

    line-height: 1.65;
  }


  /* ================================
     BOTONES
  ================================= */

  .hero-actions {
    display: flex;

    justify-content: center;
    align-items: center;

    gap: 12px;

    margin-top: 27px;
  }


  .btn-principal,
  .btn-secundario {
    display: inline-flex;

    align-items: center;
    justify-content: center;

    min-height: 45px;

    padding: 0 21px;

    border-radius: 28px;

    font-size: 0.78rem;

    font-weight: 700;

    text-decoration: none;

    transition:
      transform 0.25s ease,
      background 0.25s ease,
      color 0.25s ease,
      border-color 0.25s ease;
  }


  .btn-principal {
    gap: 8px;

    background: #C8A24A;

    color: #1B1B1B;

    border: 1px solid #C8A24A;
  }


  .btn-principal span {
    font-size: 1rem;

    transition:
      transform 0.25s ease;
  }


  .btn-principal:hover {
    transform: translateY(-2px);

    background: #d8b65f;
  }


  .btn-principal:hover span {
    transform: translateX(4px);
  }


  .btn-secundario {
    background: transparent;

    color: #F7F4ED;

    border:
      1px solid
      rgba(
        247,
        244,
        237,
        0.35
      );
  }


  .btn-secundario:hover {
    transform: translateY(-2px);

    border-color: #C8A24A;

    color: #C8A24A;
  }


  /* ================================
     INDICADOR INFERIOR
  ================================= */

  .hero-bottom {
    position: absolute;

    left: 0;
    right: 0;

    bottom: 18px;

    display: flex;

    justify-content: center;

    z-index: 2;
  }


  .hero-scroll {
    display: flex;

    align-items: center;

    gap: 8px;

    color:
      rgba(
        247,
        244,
        237,
        0.45
      );

    font-size: 0.62rem;

    letter-spacing: 0.8px;

    text-transform: uppercase;
  }


  .hero-scroll span {
    display: block;

    width: 6px;
    height: 6px;

    border-radius: 50%;

    background: #C8A24A;

    animation:
      pulse 2s infinite;
  }


  .hero-scroll p {
    margin: 0;
  }


  /* ================================
     ANIMACIÓN
  ================================= */

  @keyframes pulse {

    0%,
    100% {
      opacity: 0.4;
      transform: scale(0.8);
    }

    50% {
      opacity: 1;
      transform: scale(1);
    }

  }


  /* ================================
     TABLET / CELULAR
  ================================= */

  @media (max-width: 700px) {

    .hero {
      min-height: calc(100vh - 70px);
    }


    .hero-content {
      padding: 40px 0 65px;
    }


    .hero-category {
      font-size: 0.58rem;

      letter-spacing: 2.2px;
    }


    .hero h1 {
      font-size: clamp(
        4rem,
        20vw,
        6rem
      );

      letter-spacing: -3px;
    }


    .hero-line {
      width: 50px;

      margin:
        17px auto 20px;
    }


    .hero h2 {
      font-size: clamp(
        1.7rem,
        8vw,
        2.5rem
      );
    }


    .hero-description {
      font-size: 0.86rem;

      line-height: 1.6;
    }


    .hero-actions {
      flex-direction: column;

      width: 100%;

      margin-top: 24px;
    }


    .btn-principal,
    .btn-secundario {
      width: min(
        100%,
        270px
      );

      min-height: 43px;
    }


    .hero-bottom {
      bottom: 14px;
    }


    .hero-glow {
      width: 300px;
      height: 300px;

      right: -130px;
      top: -100px;
    }

  }


  /* ================================
     CELULAR PEQUEÑO
  ================================= */

  @media (max-width: 450px) {

    .hero-content {
      width:
        calc(
          100% - 32px
        );

      padding:
        30px 0 55px;
    }


    .hero h1 {
      font-size: 4rem;
    }


    .hero h2 {
      font-size: 1.65rem;
    }


    .hero-description {
      font-size: 0.82rem;
    }


    .hero-scroll {
      font-size: 0.55rem;
    }

  }

</style>